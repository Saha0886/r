Ниже — самый простой и понятный способ **создать и публиковать (писать) топик в ROS 2 Jazzy** для Python и C++.

ROS 2 Jazzy использует rclcpp/rclpy точно так же, как Humble или Iron, поэтому примеры подходят без изменений.

---

# ✅ **Python (rclpy)** — создание паблишера и публикация сообщения

### 1. Создаём пакет

```bash
ros2 pkg create --build-type ament_python my_py_pub
```

### 2. Открываем файл `my_py_pub/my_py_pub/my_publisher.py` и пишем код:

```python
import rclpy
from rclpy.node import Node
from std_msgs.msg import String

class MyPublisher(Node):

    def __init__(self):
        super().__init__('my_publisher')
        self.publisher_ = self.create_publisher(String, 'chatter', 10)
        self.timer = self.create_timer(0.5, self.timer_callback)
        self.i = 0

    def timer_callback(self):
        msg = String()
        msg.data = f'Hello ROS2 Jazzy: {self.i}'
        self.publisher_.publish(msg)
        self.get_logger().info(f'Published: {msg.data}')
        self.i += 1

def main(args=None):
    rclpy.init(args=args)
    node = MyPublisher()
    rclpy.spin(node)
    node.destroy_node()
    rclpy.shutdown()

if __name__ == '__main__':
    main()
```

### 3. Добавляем в `setup.py`:

```python
entry_points={
    'console_scripts': [
        'my_publisher = my_py_pub.my_publisher:main',
    ],
},
```

### 4. Собираем и запускаем:

```bash
colcon build
source install/setup.bash
ros2 run my_py_pub my_publisher
```

---

# ✅ **C++ (rclcpp)** — создание паблишера

### 1. Создаём пакет:

```bash
ros2 pkg create --build-type ament_cmake my_cpp_pub --dependencies rclcpp std_msgs
```

### 2. Пишем код `my_cpp_pub/src/my_publisher.cpp`:

```cpp
#include "rclcpp/rclcpp.hpp"
#include "std_msgs/msg/string.hpp"

class MyPublisher : public rclcpp::Node
{
public:
    MyPublisher() : Node("my_publisher"), count_(0)
    {
        publisher_ = this->create_publisher<std_msgs::msg::String>("chatter", 10);
        timer_ = this->create_wall_timer(
            std::chrono::milliseconds(500),
            std::bind(&MyPublisher::timer_callback, this)
        );
    }

private:
    void timer_callback()
    {
        auto msg = std_msgs::msg::String();
        msg.data = "Hello ROS2 Jazzy: " + std::to_string(count_++);
        RCLCPP_INFO(this->get_logger(), "Published: '%s'", msg.data.c_str());
        publisher_->publish(msg);
    }

    rclcpp::Publisher<std_msgs::msg::String>::SharedPtr publisher_;
    rclcpp::TimerBase::SharedPtr timer_;
    size_t count_;
};

int main(int argc, char * argv[])
{
    rclcpp::init(argc, argv);
    rclcpp::spin(std::make_shared<MyPublisher>());
    rclcpp::shutdown();
    return 0;
}
```

### 3. Добавляем в `CMakeLists.txt`:

```cmake
add_executable(my_publisher src/my_publisher.cpp)
ament_target_dependencies(my_publisher rclcpp std_msgs)
install(TARGETS my_publisher DESTINATION lib/${PROJECT_NAME})
```

### 4. Сборка и запуск:

```bash
colcon build
source install/setup.bash
ros2 run my_cpp_pub my_publisher
```

---

# 🔍 Проверка публикаций

В отдельном терминале:

```bash
ros2 topic echo /chatter
```

---

Если хочешь, могу написать **подписчик**, **сервис**, **action**, или сделать «скелет» полноценного ROS 2 Jazzy проекта.
# Запустите в отдельном терминале
rostopic pub /robohead_controller/voice_recognizer_pocketsphinx/cmds_recognizer/commands std_msgs/String "data: 'улыбнись'"
